extends layout

block content
  h1= title
  - let iphone  = results
  div(class="container-fluid")
    div(class="row")
    each item in results
      div(class="col-sm-2") Iphone Model:#{item.iphone_model}, iphone size:#{item.iphone_Size}, iphone Cost:#{item.iphone_Cost}
  br